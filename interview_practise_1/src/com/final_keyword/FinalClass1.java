package com.final_keyword;

public class FinalClass1 
{
   FinalClass f = new FinalClass(); // we can create object inside class also 
   // if the class is final means that class properties and methods are not private we can use those methods and properties
   public void show()
   {
	   f.show();
   }
}
